---
/**
 * The only button in the project. Renders <a> when given href, <button> otherwise,
 * so a link never pretends to be a button (or vice versa) for screen readers.
 */
interface Props {
  href?: string;
  variant?: "gold" | "ghost";
  size?: "md" | "sm";
  type?: "button" | "submit";
  id?: string;
  class?: string;
  external?: boolean;
}

const {
  href,
  variant = "gold",
  size = "md",
  type = "button",
  id,
  class: className = "",
  external = false,
} = Astro.props;

const classes = ["btn", `btn--${variant}`, size === "sm" && "btn--sm", className]
  .filter(Boolean)
  .join(" ");

const externalAttrs = external ? { target: "_blank", rel: "noopener noreferrer" } : {};
---

{
  href ? (
    <a href={href} id={id} class={classes} {...externalAttrs}>
      <slot />
    </a>
  ) : (
    <button type={type} id={id} class={classes}>
      <span class="btn__label"><slot /></span>
      <span class="btn__spinner" aria-hidden="true"></span>
    </button>
  )
}

<style>
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 9px;
    padding: 12px 22px;
    border-radius: 999px;
    border: 1px solid transparent;
    font-family: inherit;
    font-size: 0.9rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    text-decoration: none;
    cursor: pointer;
    white-space: nowrap;
    transition:
      transform 0.18s var(--ease),
      background 0.2s,
      border-color 0.2s,
      color 0.2s;
  }

  .btn:active {
    transform: translateY(1px);
  }

  .btn--gold {
    background: var(--accent);
    color: var(--on-accent);
  }

  .btn--gold:hover {
    background: var(--accent-bright);
  }

  .btn--ghost {
    border-color: var(--rule-strong);
    color: var(--text);
    background: transparent;
  }

  .btn--ghost:hover {
    border-color: var(--accent);
    color: var(--accent);
  }

  .btn--sm {
    padding: 9px 17px;
    font-size: 0.82rem;
  }

  .btn[disabled] {
    opacity: 0.55;
    cursor: not-allowed;
  }

  /**
   * Loading state. The label stays in place and keeps its width, so the button
   * doesn't jump or resize mid-submit — it just dims and gains a spinner.
   * Driven by `data-loading` so any script can set it without knowing our CSS.
   */
  .btn__label {
    display: inline-flex;
    align-items: center;
    gap: 9px;
  }

  .btn__spinner {
    display: none;
    width: 15px;
    height: 15px;
    border: 2px solid currentColor;
    border-right-color: transparent;
    border-radius: 50%;
    animation: btn-spin 0.6s linear infinite;
  }

  .btn[data-loading="true"] {
    cursor: progress;
  }

  .btn[data-loading="true"] .btn__label {
    opacity: 0.55;
  }

  .btn[data-loading="true"] .btn__spinner {
    display: block;
  }

  @keyframes btn-spin {
    to {
      transform: rotate(360deg);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .btn__spinner {
      animation-duration: 1.8s;
    }
  }
</style>
